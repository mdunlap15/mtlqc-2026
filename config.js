// Fill these in to light up the Shared (Trip) tab and the Concierge chat.
// The anon key is Supabase's *publishable* key — it is designed to ship in clients.
window.MTLQC_CONFIG = {
  SUPABASE_URL: "",        // e.g. https://abcdefgh.supabase.co
  SUPABASE_ANON_KEY: "",   // Settings → API → anon public
  CONCIERGE_URL: "",       // e.g. https://mtlqc-concierge-production.up.railway.app
  TRIP_KEY: "mtlqc",       // must match the Railway TRIP_KEY variable
  TRIP_ID: "mtlqc-2026"
};
